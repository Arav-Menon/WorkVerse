{
  "name": "@repo/stream",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsup",
    "start:stream": "node dist/bin.js",
    "dev:stream": "node --watch --import tsx ./src/bin.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@repo/rate-limit": "*",
    "@repo/schemas": "*",
    "@repo/redis": "*",
    "mediasoup": "^3.20.0"
  }
}
