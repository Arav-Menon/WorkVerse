export class CollisionSystem {
  constructor() { }

  update() {
    // Physics engine handles all collision now
    // This system reserved for future custom logic
    // (e.g., door transitions, zone-specific rules)
  }
}
